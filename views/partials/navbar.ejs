<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Yelp Camp</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <div class="navbar-nav" id="nav">
        <a class="nav-link" href="/">Home</a>
        <a class="nav-link" href="/campsites">View all campsites</a>
        <a class="nav-link" href="/campsites/new">Add new campsite</a>
      </div>
      <div class="navbar-nav d-flex ms-auto">
        <% if (!currentUser) { %>
        <a class="btn btn-outline-secondary" href="/register">Register</a>
        <a class="btn btn-outline-secondary ms-2" href="/login">Log in</a>
        <% } else { %>
        <div class="navbar-text">Logged in as <%= currentUser.username %></div>
        <a class="btn btn-outline-secondary ms-2" href="/logout">Log out</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
