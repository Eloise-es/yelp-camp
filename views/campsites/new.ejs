<% layout('layouts/boilerplate') %>
<div class="row">
  <h1 class="text-center">New Campsite</h1>
  <di class="col-6 offset-3">
    <form action="/campsites" method="POST" class="validated-form" novalidate>
      <div class="mb-3">
        <label for="title" class="form-label">Name</label>
        <input
          type="text"
          name="campsite[title]"
          placeholder="e.g. Peak View Caravan Park"
          id="title"
          class="form-control"
          required
        />
        <div class="valid-feedback">Looks good!</div>
        <div class="invalid-feedback">Please include the campsite name.</div>
      </div>

      <div class="mb-3">
        <label for="location" class="form-label">Location</label>
        <input
          type="text"
          name="campsite[location]"
          placeholder="e.g. Castleton, UK"
          id="location"
          class="form-control"
          required
        />
        <div class="valid-feedback">Looks good!</div>
        <div class="invalid-feedback">Please include the location.</div>
      </div>

      <div class="mb-3">
        <label for="image" class="form-label">Link to image</label>
        <input
          type="text"
          name="campsite[img]"
          id="img"
          placeholder="e.g. https://images.unsplash.com/..."
          class="form-control"
          required
        />
        <div class="valid-feedback">Looks good!</div>
        <div class="invalid-feedback">Please include a link to an image.</div>
      </div>
      <div class="mb-3">
        <label for="description" class="form-label">Description</label>
        <textarea
          type="text"
          name="campsite[description]"
          id="description"
          placeholder="e.g. The perfect romantic getaway in the mountains, with clean bathrooms and a range of facilities, including..."
          class="form-control"
          required
        ></textarea>
        <div class="valid-feedback">Looks good!</div>
        <div class="invalid-feedback">Please include a short description.</div>
      </div>
      <div class="mb-3">
        <label for="price" class="form-label">Price per night</label>
        <div class="input-group">
          <span class="input-group-text">£</span>
          <input
            type="text"
            class="form-control"
            id="price"
            placeholder="0.00"
            name="campsite[price]"
            aria-label="Amount (to the nearest pound)"
            required
          />
          <div class="valid-feedback">Looks good!</div>
          <div class="invalid-feedback">Please include a valid price.</div>
        </div>
      </div>
      <div class="mb-3"><button class="btn btn-success">Submit</button></div>
    </form>
    <a href="/campsites">View all campsites</a>
  </di>
</div>
